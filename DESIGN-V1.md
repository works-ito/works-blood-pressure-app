# 血圧記録アプリ V1 基本設計（固定版）

## 1. 目的
従業員が出社時等に測定した血圧・脈拍を、本人が簡単に記録し、必要な権限者だけが閲覧できる社内Webアプリを構築する。

## 2. 基本原則
- 健康情報へのアクセスは原則としてアプリ経由とする。
- Google スプレッドシートは保存領域として用い、日常運用では直接開かない。
- 血圧ログには実名を保存せず、社員番号で管理する。
- 実名対応は別マスタとし、実名閲覧権限を持つユーザーだけが参照できる。
- 権限判定はフロント画面だけでなく、必ずサーバー側でも実施する。

## 3. 認証
- 認証基盤は Auth0 を採用する。
- 通常利用は Passkey を第一選択とする。
- Passkey は「生体認証そのもの」ではなく、端末側のユーザー確認を使う認証方式として扱う。
- 端末側のユーザー確認方法は利用端末の設定に従う。例: Face ID、Touch ID、指紋、顔認証、端末PIN、パターン、Windows Hello 等。
- したがってアプリ内では「Face IDでログイン」等に固定せず、「Passkeyでログイン」を基本表記とする。必要に応じて「Face ID・指紋・端末ロック解除などを使用」と補足する。
- Passkey 非対応・未登録・利用不能時に備え、パスワードによる代替ログイン／復旧手段を残す。
- 管理者は本パスワードを閲覧できず、初期化のみ可能とする。
- 社員番号を業務上のユーザー識別子として使用する。Auth0上のログイン識別子・ユーザー属性との対応方法はPhase 0で確定する。
- 端末変更時は、パスワード等の代替認証で本人確認後、新しい端末へPasskeyを登録できる設計とする。

### Phase 0 認証PoCの現状
- Auth0 SPA連携: 確認済み。
- iPhoneでPasskey新規登録: 確認済み。
- iPhoneで保存済みPasskeyを使った再ログイン: 確認済み。
- iPhoneでは今回Face IDが使われたが、Face ID必須仕様ではない。
- Android実機でのPasskey再ログイン: 未確認。
- パスワードによる代替ログイン／復旧: 未確認。
- 社員番号をログイン識別子として運用する最終方式: 未確定。

## 4. 血圧記録
- 入力項目: 最高血圧、最低血圧、脈拍。
- 1日最大3件。
- 3件達成を要求しない。画面には「本日の記録: n件」とだけ表示する。
- 「あとn回」「未完了」等の達成判定は行わない。
- 日付・時刻の正はサーバー側 Asia/Tokyo とする。
- 登録前に確認画面を表示する。
- 最高血圧 < 最低血圧は登録不可。
- 極端な値は入力ミス確認を出す設計とし、医学的診断は行わない。
- 同時入力時もサーバー側で1日最大3件を最終検証する。

## 5. 本人画面
- 自分の当月記録を日別で一覧表示。
- 各日最大3件を時刻とともに表示。
- 日付を選択して各記録を編集可能。
- 編集前の値は変更履歴ログに保持する。
- 前月分は翌月3日 05:00 まで編集可能。
- 3日 05:00 の締め後は本人編集不可。

## 6. 権限
権限は役職名ではなく、機能単位で設定する。
- 閲覧範囲: SELF / DEPARTMENT / ALL
- 実名閲覧: ON / OFF
- CSV出力: ON / OFF
- 管理権限: ON / OFF

想定:
- 一般社員: SELF / 実名OFF / CSV OFF / 管理OFF
- 部署上長: DEPARTMENT / 実名は管理者設定 / CSVは管理者設定 / 管理OFF
- 社長: ALL / 実名ON / 管理は任意
- サービス部長（管理者）: ALL / 実名ON / CSV ON / 管理ON

## 7. ユーザー管理
管理権限ユーザーのみアプリ内で実施する。
- 社員追加
- 社員番号自動採番
- 氏名
- 部署
- 各種権限
- 利用停止 / 再開
- パスワード初期化
- 初期パスワード発行

本人は初回利用時に本人確認を行い、Passkeyを登録する。パスワード運用を併用する場合は、管理者が本パスワードを知る必要がない方式で初期設定・再設定できる設計とする。

## 8. 利用目的・同意
初回ログイン時に以下を明示する。
- 収集目的
- 収集する情報
- 閲覧可能者の範囲
- 健康管理目的以外に利用しないこと
- 医学的診断を行うアプリではないこと

保存項目:
- 社員番号
- 同意日時
- 利用目的文書のバージョン

文書更新時は再同意を要求できる設計とする。

## 9. CSV
- 期間指定で出力。
- 出力範囲はログインユーザーの閲覧権限を超えない。
- 実名閲覧OFFの場合は氏名列を含めない。
- CSV出力操作を操作ログに記録する。

## 10. 月次PDF
- 毎月3日 05:00 に前月分を締める。
- 部署別PDFを自動生成する。
- 各部署の指定 Google Drive フォルダへ自動保存する。
- 社長・管理者は全体、各上長は自部署のフォルダのみ閲覧できる既存Drive運用に合わせる。
- 管理者のみ締め後修正・PDF再生成可能。
- 再生成は最新版に更新し、再生成操作をログに残す。
- 自動生成失敗時は管理者へ通知する。

## 11. PDFの血圧表示
JSH2025 を参考に、診断ではなく確認補助として色分けする。
- 130–139 または 80–89: 注意表示
- 140以上 または 90以上: 強調表示
- 180以上 または 120以上: 強い警告表示
- 判定は OR 条件。
- 閾値は管理設定から変更可能にする。
- PDFには「色付き表示は確認を補助する目安であり、医学的診断や就業可否を示すものではない」旨を記載する。

## 12. ログ
- 血圧変更履歴
- ログイン
- 健康情報閲覧
- CSV出力
- PDF生成 / 再生成
- 権限変更
- ユーザー追加 / 停止
- パスワード初期化

## 13. 初版に含めないもの
- Bluetooth血圧計連携
- グラフ
- AI判定
- 勤怠連携
- 測定回数未達アラート
- プッシュ通知

## 14. Phase 0 合格条件
1. Auth0とWebアプリ間で認証遷移・復帰が正常に行える。
2. Passkeyを登録できる。
3. iPhoneで端末側のユーザー確認を使ったPasskey再ログインが成功する。
4. Androidで端末側のユーザー確認を使ったPasskey再ログインが成功する。
5. Passkeyを利用できない場合でも、代替認証（パスワード等）でログイン／復旧できる。
6. 端末変更時に代替認証で本人確認し、Passkeyを再登録できる。
7. 業務上の社員番号とAuth0ユーザーを一意に対応付けられる。
