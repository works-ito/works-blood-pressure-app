# 血圧記録アプリ V1 基本設計（固定版）

## 1. 目的
従業員が出社時等に測定した血圧・脈拍を、本人が簡単に記録し、必要な権限者だけが閲覧できる社内Webアプリを構築する。

## 2. 基本原則
- 健康情報へのアクセスは原則としてアプリ経由とする。
- 認証は Auth0、業務上の利用者識別は社員番号、健康データはアプリ側保存領域で分離して管理する。
- Auth0 の `user_id` は内部一意キーとしてのみ扱い、利用者画面には表示しない。
- 社員番号は `0001` から始まる連番をアプリ側で自動採番し、利用者自身には決めさせない。
- 血圧ログには実名を直接保存せず、内部キーまたは社員番号を介して管理する。
- 実名対応は別マスタとし、実名閲覧権限を持つユーザーだけが参照できる。
- 権限判定はフロント画面だけでなく、必ずサーバー側でも実施する。
- 社員番号の正本と採番処理はサーバー側の永続保存領域で管理し、同時登録時にも重複しない方式とする。
- 社員番号は一度発行したら原則再利用しない。

## 3. 認証
- 認証基盤は Auth0 を採用する。
- フロントは GitHub Pages 上の SPA とし、Auth0 SPA SDK を使用する。
- Universal Login は Identifier First を使用する。
- 通常利用は Passkey を第一選択とする。
- Passkey は「生体認証そのもの」ではなく、端末側のユーザー確認を使う認証方式として扱う。
- 端末側のユーザー確認方法は利用端末の設定に従う。例: Face ID、Touch ID、指紋、顔認証、端末PIN、パターン、Windows Hello 等。
- アプリ内では「Face IDでログイン」等に固定せず、「Passkeyでログイン」を基本表記とする。必要に応じて「Face ID・指紋・端末ロック解除などを使用」と補足する。
- 初回登録時はメールアドレスを入力し、Passkey登録を優先する。
- メールアドレスは通常の本人識別だけでなく、Passkey喪失時のセルフサービス復旧にも使用する。
- 初回から全員にパスワード設定を強制しない。
- Passkey 非対応・未登録・利用不能時は、Auth0 の Reset Password フローからパスワードを設定／再設定し、代替ログインできるようにする。
- Passkey と Password は同一 Auth0 `user_id` に共存できる構成とする。
- 管理者は本パスワードを閲覧できず、必要に応じて初期化・再設定導線のみ提供する。
- Auth0 の Client Secret は SPA / GitHub Pages に置かない。
- トークンは Auth0 SPA SDK の安全なキャッシュ機構を使用し、原則として localStorage / sessionStorage への恒久保存は行わない。

### Auth0 user_id と社員番号の役割
- `auth0|xxxx...` : 内部一意キー。認証主体とアプリ側データを結ぶための不変キー。
- `0001`, `0002`, `0003` ... : 業務上の社員番号 / 利用者ID。画面表示・検索・帳票等で使用。
- メールアドレス由来の `nickname` は社員番号として使用しない。
- 血圧データの主な紐づけキーは Auth0 `user_id` とし、社員番号変更等の影響を受けないようにする。

### 初回登録フロー
1. 利用者が「初回登録」を開始する。
2. Auth0 Universal Login へ遷移する。
3. 利用者がメールアドレスを入力する。
4. Passkey対応端末では Passkey 作成を優先する。
5. Auth0 に新規ユーザーが作成される。
6. SPAへ戻った後、サーバー側APIへ初回登録完了を通知する。
7. サーバー側で `0001` から始まる社員番号を競合なく自動採番する。
8. `Auth0 user_id ↔ 社員番号` の1対1対応を保存する。
9. アプリには Auth0内部IDではなく社員番号を表示する。

### 通常ログインフロー
1. 利用者がログインを開始する。
2. Auth0 Universal Login へ遷移する。
3. 保存済みPasskeyがある場合は Passkey を優先して認証する。
4. iPhone / Android / PC の端末側ユーザー確認を行う。
5. 認証後、アプリへ復帰する。
6. サーバー側で Auth0 `user_id` に紐づく社員番号・権限を取得する。

### 復旧フロー
1. Passkeyが利用できない場合、「ログインできない」「パスワードをリセット」等を選択する。
2. 登録済みメールアドレス宛に Auth0 の Reset Password メールを送信する。
3. メール内リンクから新しいパスワードを設定する。
4. パスワードで同一 Auth0 `user_id` にログインする。
5. 必要に応じて新しいPasskeyを再登録する。

### Phase 0 認証PoCの現状
- Auth0 SPA連携: 確認済み。
- Auth0 Universal Login日本語化: 確認済み。
- iPhoneでPasskey新規登録: 確認済み。
- iPhoneで保存済みPasskeyを使った再ログイン: 確認済み。
- iPhoneでは今回Face IDが使われたが、Face ID必須仕様ではない。
- Passkey登録済みユーザーに後からPasswordを設定し、同一 Auth0 `user_id` でログイン: 確認済み。
- Reset Passwordメールから新しいPasswordを設定する復旧導線: 確認済み。
- Android実機でのPasskey再ログイン: 未確認。
- 社員番号の自動採番API / 保存方式: 未実装。

## 4. 血圧記録
- 入力項目: 最高血圧、最低血圧、脈拍。
- 1日最大3件。
- 3件達成を要求しない。画面には「本日の記録: n件」とだけ表示する。
- 「あとn回」「未完了」等の達成判定は行わない。
- 日付・時刻の正はサーバー側 Asia/Tokyo とする。
- 登録前に確認画面を表示する。
- 最高血圧 < 最低血圧は登録不可。
- 極端な値は入力ミス確認を出す設計とし、医学的診断は行わない。
- 同時入力時もサーバー側で1日最大3件を最終検証する。

## 5. 本人画面
- 自分の当月記録を日別で一覧表示。
- 各日最大3件を時刻とともに表示。
- 日付を選択して各記録を編集可能。
- 編集前の値は変更履歴ログに保持する。
- 前月分は翌月3日 05:00 まで編集可能。
- 3日 05:00 の締め後は本人編集不可。

## 6. 権限
権限は役職名ではなく、機能単位で設定する。
- 閲覧範囲: SELF / DEPARTMENT / ALL
- 実名閲覧: ON / OFF
- CSV出力: ON / OFF
- 管理権限: ON / OFF

想定:
- 一般社員: SELF / 実名OFF / CSV OFF / 管理OFF
- 部署上長: DEPARTMENT / 実名は管理者設定 / CSVは管理者設定 / 管理OFF
- 社長: ALL / 実名ON / 管理は任意
- サービス部長（管理者）: ALL / 実名ON / CSV ON / 管理ON

## 7. ユーザー管理
管理権限ユーザーのみアプリ内で実施する。
- 社員追加
- 社員番号自動採番
- 氏名
- 部署
- 各種権限
- 利用停止 / 再開
- パスワード初期化 / 再設定導線
- Auth0 user_id と社員番号の対応管理

### 社員番号自動採番
- 初回の正式採番は `0001` から開始する。
- 保存上は整数 `1, 2, 3...` とし、表示時に `0001` のようにゼロ埋めする。
- 10000以上になった場合は5桁以上へ自然拡張し、固定4桁上限とはしない。
- 退職・削除等で欠番が発生しても原則再利用しない。
- フロント側で「最大値 + 1」を計算しない。
- サーバー側のトランザクション / ロック / Sequence 相当の仕組みで重複発行を防ぐ。
- Auth0 Action 内だけで連番カウンタを持つ方式は採用しない。

## 8. 利用目的・同意
初回ログイン時に以下を明示する。
- 収集目的
- 収集する情報
- 閲覧可能者の範囲
- 健康管理目的以外に利用しないこと
- 医学的診断を行うアプリではないこと

保存項目:
- 社員番号
- 同意日時
- 利用目的文書のバージョン

文書更新時は再同意を要求できる設計とする。

## 9. CSV
- 期間指定で出力。
- 出力範囲はログインユーザーの閲覧権限を超えない。
- 実名閲覧OFFの場合は氏名列を含めない。
- CSV出力操作を操作ログに記録する。

## 10. 月次PDF
- 毎月3日 05:00 に前月分を締める。
- 部署別PDFを自動生成する。
- 各部署の指定 Google Drive フォルダへ自動保存する。
- 社長・管理者は全体、各上長は自部署のフォルダのみ閲覧できる既存Drive運用に合わせる。
- 管理者のみ締め後修正・PDF再生成可能。
- 再生成は最新版に更新し、再生成操作をログに残す。
- 自動生成失敗時は管理者へ通知する。

## 11. PDFの血圧表示
JSH2025 を参考に、診断ではなく確認補助として色分けする。
- 130–139 または 80–89: 注意表示
- 140以上 または 90以上: 強調表示
- 180以上 または 120以上: 強い警告表示
- 判定は OR 条件。
- 閾値は管理設定から変更可能にする。
- PDFには「色付き表示は確認を補助する目安であり、医学的診断や就業可否を示すものではない」旨を記載する。

## 12. ログ
- 血圧変更履歴
- ログイン
- 健康情報閲覧
- CSV出力
- PDF生成 / 再生成
- 権限変更
- ユーザー追加 / 停止
- パスワード初期化
- 社員番号採番

## 13. 初版に含めないもの
- Bluetooth血圧計連携
- グラフ
- AI判定
- 勤怠連携
- 測定回数未達アラート
- プッシュ通知

## 14. Phase 0 合格条件
1. Auth0とWebアプリ間で認証遷移・復帰が正常に行える。✅
2. Passkeyを登録できる。✅
3. iPhoneで端末側のユーザー確認を使ったPasskey再ログインが成功する。✅
4. Androidで端末側のユーザー確認を使ったPasskey再ログインが成功する。未確認
5. Passkeyを利用できない場合でも、Passwordでログイン／復旧できる。✅
6. Reset PasswordメールからPasswordを設定できる。✅
7. Auth0 Universal Loginが日本語で利用できる。✅
8. Auth0 `user_id` と業務上の社員番号を一意に対応付けられる。設計確定・未実装
9. 社員番号を `0001` から競合なく自動採番できる。未実装
10. Auth0内部IDやメール由来nicknameを社員番号として画面表示しない。未実装
